# Offline checks for the SC engine (no audio hardware, no GUI)

These scripts run the F2 engine under `sclang` and render audio with scsynth in
non-realtime mode, so the DSP can be verified on a machine with no sound card.
They are a safety net for changes to `sc/f2units.scd`: they catch syntax errors,
UGen-graph errors, silent units and NaN/blow-ups before the engine is loaded in
the app.

## Setup (Debian / Ubuntu)

    apt-get install -y --no-install-recommends supercollider-language supercollider-server

Run as a normal user (not root — Qt's sandbox refuses) with an offscreen
platform. Paths resolve from the script itself, so the working directory does
not matter; renders go to `$F2_NRT_OUT` (default `/tmp`), never into the repo:

    QT_QPA_PLATFORM=offscreen sclang -d /tmp -i none sc/nrt/compile.scd
    F2_NRT_OUT=/tmp/f2nrt QT_QPA_PLATFORM=offscreen sclang -d /tmp -i none sc/nrt/chain.scd

`headfwd.scd` renders nothing and needs no output directory — it opens the
front's UDP port itself and exits with its own verdict.

`live-banks.scd` is the exception to "no audio hardware, no server": it NEEDS a
live scsynth, because what it checks cannot exist offline. An NRT score is one
thread executing messages in timestamp order, while a real server hands
`/b_alloc` to its non-realtime thread and executes `/b_setn` immediately on the
realtime one — so a fill sent straight after an allocation lands on a buffer that
does not exist yet and is dropped without a word. Start any server and point the
script at it (no audio device is needed, and `-l 1` is not optional — without it
sclang's login handshake reports 64 clients and each client's slice of the buffer
numbers is too small for f2units to load at all):

    scsynth -u 57110 -a 1024 -b 4096 -l 1 -i 0 -o 2 &
    F2_LIVE_PORT=57110 QT_QPA_PLATFORM=offscreen sclang -d /tmp -i none sc/nrt/live-banks.scd

It reads `beat.wav` from `F2_NRT_SAMPLES` (default: the directory of the script),
renders nothing and prints its own verdict.

`fragments.scd` needs no sample files at all: the three test buffers it wants — a
click, a noise burst and a chirp — are cheaper to build inside the score than to
read, and building them there keeps the check independent of `mksamples.py`.

`sampler.scd`, `sliced.scd` and `cycles.scd` read the generated samples from
`F2_NRT_SAMPLES` (default: the directory of the script), so run `mksamples.py`
there first.
`sliced.scd` also RENDERS the slicing analysis: it spawns its own short NRT
passes over `beat.wav` and `line.wav` (the same ones `~f2SmpRes` kicks in the
app) and writes their scratch files into `F2_NRT_OUT` before the main render.

## The checks

| script | what it proves | how to read it |
|---|---|---|
| `compile.scd` | `f2units.scd` and `f2dsl.scd` parse | prints `syntax OK` per file |
| `build.scd` | every SynthDef builds; `polysynthU`, `samplerU` and `polymerU` expose their contract control lists; every unison variant builds, every advertised filter model on every unit (the §flt union: all eleven built on all three — no `*_filt_*` fallback may appear, and `~f2FltBuilt` must equal the menu with each unit's default first) and every advertised envelope model (the §env union: six built on every unit — `shot` on the synths with `hold` as its body — `segments` alone falling back), and so does every one of the 693 advertised polymer combinations — each declaring the SAME controls as its base def, with one fallback warning per model a later stage still owes; the shared filter table is checked to be the same on the synths and its core on the sampler, the §3.12 configuration lists (`~f2FltModeSel` / `~f2FltMode2Sel`) to carry the contract's counts, the shared envelope table and menu on all three, and the base def names to be unchanged (the default `env` never reaches a variant name). Then §cut the ROSTER after units v49: the three cut units (`amU`, `envcaU`, `onsetU`) are gone from the lib and each names a successor, 26 units remain (23 written out + the three generated), the thirteen processors are all present and all carry the four bands' controls (`chainIn` + the IN three + the ADSR five + the EQ five + `amp`), the line a save built on a cut unit gets is printed verbatim, and convU's two `ir` modes are checked: the keyless card resolves to the BASE def, the resource opts `irSample` / `irLen` stay out of the variant name, both variants declare the SAME control set including `preDly` and `irBuf`, and one partition of IR normalises to exactly 1.0 (which is what makes the null IR a pass-through) | prints the control list and the byte size per variant (the sampler's eleven filter variants give each arm's cost in bytes over the `poly` base), then the schema counts: 72 live polysynth, 92 live sampler and 81 live polymer controls + `freq`, checked against the spec tables from both ends |
| `envab.scd` | §env the A/B of the ONE amplitude envelope (ENVELOPE.md §4.7), of the sampler's filter-block join (FILTER.md §5.3) and of the `ld` arm (FILTER.md §3.1: its 24 dB tap IS MoogFF through res 0.9 — a polymer save at its default model with the resonance up, and a polysynth `filt: ld` save at `fltMode` 0, render bit for bit what they did; the polysynth's own `fltMode` default 1 under `ld` is the 18 dB tap on the raw def, the change the case `poly_ld_tap` asserts is present and prints — it is WHY the front's load path pins every pre-arm `ld` save to `fltMode` 0, `src/lib/ldTapMigrate.ts`, so a migrated save renders `poly_ld`), plus the synths' `shot` (a one-shot that ignores its gate and ends on its own, on polymerU and polysynthU): layer 1 asserts, in sclang with no server, that the fragment's Env at each unit's defaults and at the saved-style value sets is the Env the unit built before, array for array; layer 2 renders the same notes through a REFERENCE `f2units.scd` (`F2_NRT_REF`, the parent commit's — required) and the tree's and compares them sample for sample, then checks the pool (a pooled voice under `pluck` takes a second gate) and the model shapes on polymerU | prints max \|Δ\|, the RMS of Δ, the first differing sample and Δ over the sustain per case against the bound each case carries (0 where the graph did not change, the derived rate-move bound on polysynthU / samplerU-gated; `F2_ENV_RATE_MOVED=0` asks for 0 everywhere), then `envab OK` |
| `sweep.scd` | 42 polysynth settings render: the 7 `poly` filter modes and the bypass, 7 shaper modes, 6 blend operators, 3 high-pass modes, shape / sub / sync / octave, unison width, feedback, filter FM, drive, noise, and an all-maxed case. Then §pm's `cut` / `res` grid — 7 cutoffs × 6 resonances plus the corners (the ladder in its own resonance, the FEG ±120 st, keytrack, the bypass) and the five §6.2 oscillators at the ends of their own knobs (Union all three at 1, Shape / Skew / Fold / PW / Sync at their limits, one at 55 Hz, one stereo-detuned backwards). Then §flt, FILTER.md §3.0's verification per arm: every (`fltMode`, `fltMode2`) entry of every filter model — 91 over the eleven — on every unit: on polymerU a magnitude sweep (seven `osc: sine` notes at fc·2^k against the bypass, the entry's shape asserted — LP / HP / BP / notch / bell / comb / formant — and the slope order of the ladder taps and pole counts), a self-oscillation settle (a 50 ms noise kick into a silent Union at res 1 under a held AEG: no growth between 0.25…0.45 s and 0.75…0.95 s) and a res 1 + drive +24 dB stress (no NaN, bounded, still sounding), plus both ends of the cutoff knob per model; on polysynthU (the ar bus, `fb` 1, `filtFm` 1) and samplerU (a noise loop built inside the score) the stress per entry and a noise-band magnitude check per model. Then §conv, convU's two modes in one render (four output channels, the two sides of each claim measured against each other inside one pass): `preDly` at 0 against the same graph at 0.05 s must be EXACTLY 2400 samples apart at 48 k (2399 would mean the delay line is always in circuit — DelayN clamps a non-constant delaytime to one sample); the SAMPLE mode's latency is PartConv's partition, 960 samples at fft 2048, measured against the same graph's own dry path so the port's block delay cancels; a prepared IR of a single 1.0 is a pass-through (the reason `~f2ConvIrNull` can stand in while a real IR is being prepared); a real four-partition IR off `beat.wav`, levelled by `~f2ConvIrNorm` exactly as the engine levels it, sounds and stays bounded; and `irBuf` below zero is EXACTLY silent | renders `sweep.aiff`; each case is 0.6 s apart, none may be silent and none may be NaN. The polymer grid renders `pmsweep.aiff` in FLOAT (an int16 file cannot carry a NaN) and measures itself: no NaN, no divergence, every case still sounding, then `polymer sweep OK`. The filter sweep renders `flt_<unit>.aiff`, one MONO float file per unit, read back window by window, and prints one line per case with its numbers, then `§flt sweep OK`. §conv renders `conv.aiff` (4 ch float) and prints its five claims as numbers, then `§conv sweep OK` |
| `convab.scd` | §conv-ir that units v49's SAMPLE mode cost convU's LIVE BUS CAPTURE nothing. v49 gave the unit a second graph (`ir: sample`, PartConv over a prepared spectrum buffer) and a new live control (`preDly`), and the owner's answer on the bus mode was «continuous, as it is now» — so the kernel-capture path must render SAMPLE FOR SAMPLE what it rendered before. Four cases through a real transient off `beat.wav` (two kernel lengths, a mid-note `t_trig` recapture, and the conditioner + EQ off their defaults) are rendered through a REFERENCE `f2units.scd` (`F2_NRT_REF`, the parent commit's — required, the script refuses to compare a file to itself) and through the tree's, in one process. What it catches: `DelayN` with a non-constant delaytime clamps to a ONE-SAMPLE minimum, so the obvious spelling of `preDly` — the delay always in circuit — puts the whole bus render one sample late; that is why the stage is a `Select.ar` | prints max \|Δ\|, the first differing sample, and the reference peak / RMS per case; every max \|Δ\| must be exactly 0, then `ALL OK — the bus mode is bit-identical` |
| `procab.scd` | §proc that the cut changed nothing it did not set out to change. `convab` guards convU's bus mode; this one guards the OTHER TWELVE — a units file is one file, and the edits that removed three SynthDefs, added a generator and bumped `~f2UnitsVer` all ran through it. It renders a real STRIP, not a unit in isolation: a source into three processors chained through private stage buses exactly as `chainStages` wires them, each reading its stream with `In.ar` through `chainIn`. The three are picked for different machinery — combU is a feedback delay line, foldU the nonlinear stage plus a sample-rate latch, shiftU the SSB Hilbert pair — and the fourth case is vocU, the two-input unit that took amU's place in every fixture, with its modulator on an external port. Then, on the tree's own build, the claim the GREYED envelope band is drawn from: two slots of one strip differing ONLY in `atk dec sus rel curve` (gate-off inside the slot), each preceded by a full slot of silence so the previous slot's bus residue cannot be mistaken for a live envelope | prints max \|Δ\|, the first differing sample and the reference peak / RMS per case; every max \|Δ\| must be exactly 0 over a NON-SILENT slot, then `ALL OK — the other twelve render sample for sample as before` |
| `chain.scd` | the §cards strip semantics: source → processor reading `chainIn` → `f2chainOut` sink is audible, a processor with `chainIn = -1` is exactly silent, a bypassed middle card still plays | renders `chain.aiff` with the three cases at 0.1 s / 2.0 s / 3.2 s |
| `units.scd` | every processor that takes `chainIn` passes the stream through | renders `units.aiff`, one processor per 0.8 s slot |
| `mksamples.py` + `analyze.scd` | the analysis the Sampler needs — onset detection and pitch tracking over a sample — runs offline and gives usable results | writes `beat-analysis.wav`, a three-channel float file: onset trigger, detected pitch, pitch confidence |
| `headfwd.scd` | §smp stage 2, the LIVE half of the playhead telemetry: `OSCdef(\f2headFwd)` is installed on `/f2_head` for the server address, maps the reply id to the preset deploy key through `~f2TelMap` and forwards `[dk, vi, ci, pos, span, mode]` to the front's socket, keeping the two sampler cards of one preset apart by `ci` — and stays silent when the map or the address is missing | boots nothing (no scsynth, no audio device): prints one line per case and `headfwd OK` when all of them behave |
| `sliced.scd` | §smp `samplerU` stage 3, SLICED: the slicing analysis (one NRT pass per file → onsets, the pitch track, the file tempo), the five `slicing` modes over that one result, **the payload the front is handed** (`~f2SmpAnalysisCtx` → `ctx.sampleAnalysis`: the same onsets in 0…1 of the file, their strengths, the tempo, the length — and the card's `onsetSens` filter reproducing `~f2SmpOnsetBounds` at every sensitivity), the slice table as a buffer, `~f2SmpRes` handing it to a spawn and refilling it in place, **the manual boundaries of the `cuts` opt** (decoded, keyed into the entry and PLAYED), the `slice` latch, the blue markers and the four `slicePlay` modes | three passes — the analysis and its tables, the sliced variants' graphs, then `sliced.aiff` measured case by case: where the peak of a slice sits (every slice of an eight-way division starts within 5 ms of a transient), whether a mode goes silent at the slice boundary, a loop's envelope period (0.125 s one-way, 0.25 s ping-pong), the pitch a slice plays and whether two cases are the SAME render (which is how the latch is proved, and how a manual boundary is proved to have moved the grid: with one extra boundary inside slice 0 the table's slice 2 renders bit for bit what slice 1 rendered without it). Prints `sliced OK` when every check behaves |
| `fragments.scd` | §smp `samplerU` STAGE 6, FRAGMENTS (its noise buffer is sclang's own seeded `rand2`, not an LCG line — `(1103515245 · st + 12345) % 2^31` overflows sclang's 32-bit Integer into a −1.6…−0.8 DC-biased buffer, which was five of this check's six red probes; the sixth, `motion 0`, reads a zero-crossing pitch of 18 kHz on the chirp and is still open): that the mode is advertised in the `play` opt and builds in every combination (frozen, sliced, one-shot, both caps) with the same 96 controls and the right `/f2_head` constants, that the `grains` opt reaches `GrainBuf`'s `maxGrains` as a literal, and then the engine itself — the trigger rate against `density / grainSize`, the uncoupled Grain Rate against the size that no longer moves it and against the BEAT unit that does, the cap holding the SAME controls to two different rates in two `grains` variants, Motion against the spread of the grains' own pitches, `repeats` against how many adjacent grains are the same grain with Repeat at Initial Position on and off, grain direction against where the burst lands INSIDE the grain (forwards 20 % in, backwards 80 %) and `ALT` against its alternation, and Grains Survive Voice End against where the cloud actually stops | builds its own click / noise / chirp buffers inside the score (it reads nothing `mksamples.py` writes), renders `fragments.aiff` one case per second and MEASURES it — pulse rate, pulse phase, per-grain pitch spread, grain-to-grain correlation, tail length. Prints `fragments OK` when every case behaves |
| `live-ptrs.scd` | **needs a running scsynth** (with `live-banks.scd`, the second here that does): the '/f2_head' PACKET itself, which NRT cannot see — it can render audio but has nobody to answer a SendReply to. That a variant reports 5 fixed values plus its OWN read pointers (1 repitch, 2 cycles, 6 for either cloud); that as many are live AT ONCE as the mode really has, Textures' overlap being `repeats` by construction; that the head and its pointers are in DIFFERENT places at one instant; and that Fragments' pointers walk BOTH WAYS, the sign of a slot being that grain's direction | spawns one voice per case straight on the server, grabs the raw SendReply with an OSCdef and measures the packets. Prints one line per case with its last packet, then one per assertion, and `live-ptrs OK` when all of them behave |
| `live-banks.scd` | **needs a running scsynth**: that what f2units.scd writes into server buffers at load time ACTUALLY LANDS — the 16 grain windows, the 12 saw wavetables, polymer's 12 triangle + 12 half-sine wavetables and a `~f2SmpRes` slice table are read back OFF THE SERVER and must not be empty — and that a starved buffer allocator costs at most one bank instead of aborting the file and taking `polysynthU` / `samplerU` / `polymerU` with it | prints one line per check and `live-banks OK` when all of them behave |
| `cycles.scd` | §smp `samplerU` stage 5, CYCLES: that the mode plays the NOTE and not the head — the same note at four speeds (0.5 / 1 / 2 / 4) comes out at one frequency, a different note comes out at that note, and `keytrack 0` comes out at the root; that `formant` reads the extracted waveform faster or slower while that pitch stays put; that each character moves the spectrum the way [RN] describes it — the bipolar `width` shifting the harmonic balance, `split` isolating the odd or the even harmonics, `Fund` putting the fundamental back, the phase modulator growing sidebands and `Harm` pushing them further out; and that Crossfade Two Voices removes the step the single reader makes when it lifts its next waveform. Plus the `/f2_head` reply of every Cycles variant: play code 1, the cycle window as `span`, and the latched slice above bit 5 in a sliced one | renders `cycles.aiff` (float), then measures it with its own arithmetic — an autocorrelation period for the pitch, short-segment Goertzel combs for the harmonic profile and its centroid (a long transform integrates away the phase the snapshot rotates), and the peak first difference against its median for the step. One line per case, then one line per assertion; prints `cycles OK` when every one of them behaves |
| `rootkey.scd` | §smp `samplerU` stage 4, ANALYZED ROOT KEY: `~f2SmpPitchCurve` on its own (unvoiced hops holding the last pitch, the silent head back-filled with the first, a hop under the amplitude floor not counting as a pitch, no pitch at all giving no curve, and a long file thinned to `~f2SmpPitchMax` with both ends intact), and then the graph: the analyzed root holds ONE pitch — the note's — across a file that steps through four, while the manual root plays the material's own four; the notes then attack on the rate that flattening asks for (Repitch coupling rate to pitch, §2) where the manual root keeps them 1/rate apart; `keytrack 0` is still the natural pitch; `pitchBuf = -1` still falls back to the manual root; and a Textures cloud sprayed by Motion across three notes of the file lands on the NOTE, which is the per-GRAIN correction and not the head's | builds the pitch curve of `line.wav` analytically (mksamples.py is the ground truth — the graph is what is under test, not the tracker) and hands it to the score as a buffer, renders `rootkey.aiff` one case per 2.4 s and measures it: each case's four measurement windows are placed by ITS OWN rate law, so a rate that is wrong puts the window in the wrong note and the pitch assertion falls with it. Autocorrelation for the tape pitches, an envelope-rise onset detector for the timing, and a short-window Goertzel tonality for the grain cloud (a cloud retriggers with a random phase, so a long transform integrates it away). Prints `rootkey OK` when every check behaves |
| `sampler.scd` | §smp `samplerU` stage 1: the shared phase engine (window, `reverse`, `speed`, the three loop modes with crossfade, the three at-note-off options, `freeze`, the per-cell `t_trig` re-anchor), both play modes with the three Repitch characters and the Textures cloud, and both envelope variants | renders `sampler.aiff`, one case per second, then MEASURES it: peak / RMS per probe window against `sound` / `silent` expectations. It also asserts the `/f2_head` reply **as built** — present in every variant, sharing the 30 Hz gate of `/f2_chan` (which is that gate narrowed to voice 0 of card 0, so the level channel keeps one writer), `telId` as the reply id, `vi` and `ci` as the first two values, `pos` clipped into 0…1 and `span` / `mode` the exact constants of the variant — because NRT can render audio but cannot answer a SendReply. Prints `probes failed: 0` and `/f2_head graph checks failed: 0` when every case behaves |

Analyse the renders with any tool that reads AIFF; the useful measures are peak,
RMS per slot and a NaN / clipping count.

## Test samples

`mksamples.py` writes three short WAV files with no dependencies beyond the
standard library, chosen so every slicing and play mode has something to chew on:
`beat.wav` (eight transients on a 120 bpm sixteenth grid), `line.wav` (four
pitches with a glide in the middle) and `pad.wav` (a stereo drone). They are
generated rather than committed, so the repository stays free of binaries. The
checks read them MONO (`/b_allocReadChannel`, the shape `~loadSamples` gives the
engine): `GrainBuf` needs a mono buffer and a tape reader would interleave the
channels of a stereo one.

## Known non-failures

Four processors are silent when fed only by `chainIn`, by design, and sound as
soon as their other input exists:

- `convU` needs a kernel buffer on `kerIn`
- `vocU`, `xspecU` need a modulator on `modIn`
- `pluckU` plucks on the GATE opening, so it must be spawned with `gate 0` and
  opened afterwards (which is exactly what the voice pool does)

## Notes

- The scripts pre-seed `~f2FlatWin` with a bare `Buffer` because
  `Buffer.loadCollection` needs a running server; the wavetable bank is
  reproduced as `/b_alloc` + `/b_gen sine1` messages inside the score.
- Every `polysynthU` def is larger than 16383 bytes, and so is `polymerU`'s
  base def since its `ld` arm carries the taps and the AGC (11877 → 18560
  bytes), so sclang writes a def file and `/d_load`s it instead of `/d_recv` —
  fine for a local server, which is what F2 uses. The sampler's keyless base
  stays under the line (16158); its heavier filter variants (`vowels` +13.6 kB,
  `ripple` +8.7 kB, `xp` +6.4 kB, `ld` +5.8 kB) cross it.
